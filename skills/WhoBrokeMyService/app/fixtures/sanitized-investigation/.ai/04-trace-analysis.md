# Trace Analysis

## Query: Compare worker configuration versions

The sanitized query groups responses by worker and configuration version.

## Key observations

One worker was one version behind after the update.

## Query: Inspect invalidation delivery

The delivery trace showed no event for the affected partition.
